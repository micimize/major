{
  "name": "todo_app_backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "yarn workspaces foreach --parallel --exclude worker run dev "
  },
  "resolutions": {
    "graphql": "14.x"
  },
  "devDependencies": {
    "@types/express": "^4.17.4",
    "typescript": "^3.8.3"
  },
  "workspaces": {
    "packages": [
      "db",
      "server",
      "worker"
    ]
  }
}
