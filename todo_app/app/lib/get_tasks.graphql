query GetAllTasks {
  tasks: allTasks(orderBy: CREATED_DESC) {
    nodes {
      __typename
      id
      lifecycle
      title
      description
      created
      updated
      stopwatchValue {
        start
        end
      }
    }
  }
}
