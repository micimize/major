query GetAllTasks {
  tasks: currentTasks(closedBuffer: { hours: 2 }) {
    nodes {
      __typename
      id
      lifecycle
      title
      description
      created
      updated
      closed
      stopwatchValue {
        start
        end
      }
    }
  }
}
