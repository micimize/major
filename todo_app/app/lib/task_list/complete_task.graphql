# TODO updateTaskById => updateTask in postgraphql
mutation completeTask($taskId: UUID!) {
  updateTask(input: { id: $taskId, patch: { lifecycle: COMPLETED } }) {
    clientMutationId
    task {
      __typename
      id
      lifecycle
      title
      created
      updated
    }
  }
}
